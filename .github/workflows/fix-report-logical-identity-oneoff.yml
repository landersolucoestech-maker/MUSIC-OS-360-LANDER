name: Fix report logical identity SQL (one-off)

on:
  push:
    branches: [dev]
    paths:
      - scripts/fix-report-logical-identity.py
      - .github/workflows/fix-report-logical-identity-oneoff.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/fix-report-logical-identity.py
      - name: Commit fix
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/api/src/modules/reports/import/import-commit.service.ts
          git commit -m "fix(reports): resolve logical identity to physical column"
          git push origin HEAD:dev
