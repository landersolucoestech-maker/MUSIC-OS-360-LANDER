# MUSIC OS 360 — PRIORIDADE ABSOLUTA
# ATIVAR IMEDIATAMENTE TODA A INFRAESTRUTURA DE AGENTS / SKILLS / WORKFLOWS
# ANTES DE PROSSEGUIR COM QUALQUER OUTRA TAREFA

PARE qualquer trabalho de produto neste momento.

A PRIMEIRA E ÚNICA prioridade agora é:

# DESCOBRIR, VALIDAR E ATIVAR CORRETAMENTE TODA A INFRAESTRUTURA EXISTENTE EM `.claude/`.

Não continue MusicChat.
Não continue Integrations.
Não continue auditoria de produto.
Não continue backlog.

Primeiro coloque a infraestrutura de engenharia para funcionar.

======================================================================
1. NÃO INVENTAR COMANDO DE ATIVAÇÃO
======================================================================

NÃO assuma que existe:

`activate-all`
`agents start`
`run-all-agents`
ou qualquer outro comando.

Descubra o mecanismo REAL existente no projeto.

A árvore `.claude/` confirmada possui:

1. agents
2. compat
3. contracts
4. docs
5. evaluations
6. gates
7. launchers
8. ops
9. policies
10. rules
11. runtime
12. skills
13. workflows

Use essa estrutura como source of truth.

======================================================================
2. DESCOBRIR O ENTRYPOINT REAL DE ATIVAÇÃO
======================================================================

Investigue primeiro:

`.claude/launchers/`
`.claude/runtime/`
`.claude/workflows/`
`.claude/agents/`
`.claude/skills/`
`.claude/settings*`
`.claude/contracts/`

Procure:

- launcher principal;
- orchestrator;
- mission controller;
- agent loader;
- skill loader;
- workflow runner;
- registry;
- manifest;
- CLI;
- package scripts;
- Node scripts;
- shell scripts;
- commands documentados;
- commands realmente implementados.

Faça repo-wide search por termos como:

`agents`
`skills`
`launcher`
`orchestrator`
`workflow`
`spawn`
`invoke`
`registry`
`discover`
`activation`
`activateAgents`
`loadAgents`
`runAgent`
`runWorkflow`

Não conclua pela documentação.

Confirme pelo código executável.

======================================================================
3. INVENTÁRIO DOS AGENTS — AGORA
======================================================================

Descubra a quantidade REAL.

Reporte:

`DISCOVERED_AGENT_COUNT = N`

A expectativa informada pelo usuário é aproximadamente 31.

Se o filesystem mostrar 31:

liste os 31.

Se mostrar quantidade diferente:

use a quantidade REAL.

Para cada agent determine:

- nome;
- arquivo;
- role;
- model;
- tools;
- native availability;
- invocation method;
- write capability;
- Bash capability;
- DB mutation capability;
- dependencies;
- status.

Não conte helper como agent.

======================================================================
4. INVENTÁRIO DAS SKILLS — AGORA
======================================================================

Descubra:

`DISCOVERED_SKILL_COUNT = M`

Liste todas.

Para cada:

- nome;
- localização;
- activation mechanism;
- consuming agents;
- tools;
- references;
- status;
- se pode ser invocada realmente nesta sessão.

Não assuma 10.

======================================================================
5. VALIDAR AGENTS NATIVOS
======================================================================

Antes de declarar qualquer agent ativo:

tente invocar UM agent real descoberto na pasta usando o mecanismo nativo disponível nesta sessão.

Resultado deve ser classificado:

`NATIVE_CUSTOM_AGENT`

somente se a plataforma realmente aceitar aquele `subagent_type`/mecanismo.

Se rejeitar:

NÃO declare o agent como nativo.

Classifique:

`NATIVE_CUSTOM_AGENT_NOT_SUPPORTED`

e verifique o mecanismo oficial alternativo disponível no projeto.

Se a única alternativa viável for usar general-purpose com o prompt do role:

declare explicitamente:

`GENERAL_PURPOSE_EMULATION — <agent-name>`

Nunca chame emulação de agent nativo.

======================================================================
6. VALIDAR SKILLS DE VERDADE
======================================================================

Invoque uma skill real descoberta em `.claude/skills`.

Prove que:

- foi localizada;
- SKILL.md/definition foi carregada;
- execução ocorreu;
- output foi retornado.

Depois confirme o mecanismo para as demais.

======================================================================
7. VALIDAR LAUNCHERS E WORKFLOWS
======================================================================

Não ignore `.claude/launchers`.

Determine quais launchers são realmente executáveis.

Para cada launcher relevante:

- command;
- args;
- process model;
- agents utilizados;
- workflows utilizados;
- state/journal writes;
- parallelism;
- cleanup.

Também determine quais workflows estão operacionais.

Não execute launcher destrutivo.

======================================================================
8. NÃO CORROMPER NOVAMENTE O JOURNAL
======================================================================

ATENÇÃO:

o control plane já apresentou múltiplos:

`prevHash mismatch`

e:

`state/journal integrity mismatch`.

Portanto a ativação inicial deve ocorrer com:

# CONTROL_PLANE_SINGLE_WRITER_MODE = ON

Até concorrência estar comprovadamente corrigida:

- somente 1 agent executando por vez;
- sem batch paralelo;
- sem Agent Teams paralelos;
- sem 31 subprocessos simultâneos;
- sem writers concorrentes do journal.

“Todos os agents ativos” NÃO significa “todos simultaneamente executando”.

Significa:

- todos descobertos;
- todos validados;
- todos roteáveis;
- todos disponíveis para o orchestrator;
- todos podendo ser chamados automaticamente quando aplicáveis.

======================================================================
9. TESTAR O PRIMEIRO AGENT END-TO-END
======================================================================

Escolha um agent READ_ONLY de baixo risco.

Fluxo:

DISCOVERY
→ INVOCATION
→ OUTPUT
→ JOURNAL
→ STATE
→ COMPLETION.

Depois rode:

`node .claude/runtime/ops.mjs show`

ou mecanismo equivalente já comprovado.

Confirme:

- journal íntegro;
- state íntegro;
- agent output registrado;
- nenhuma corruption.

======================================================================
10. TESTAR UM SEGUNDO ROLE
======================================================================

Execute outro agent SEQUENCIALMENTE.

Confirme novamente journal/state.

Se continuar verde:

marque:

`SEQUENTIAL AGENT EXECUTION VERIFIED`.

======================================================================
11. ATIVAR O ORCHESTRATOR AUTÔNOMO
======================================================================

Depois da validação:

configure/use o mecanismo EXISTENTE para que o Team Lead/Orchestrator:

1. receba uma tarefa;
2. classifique domínio;
3. escolha agents aplicáveis;
4. invoque skills aplicáveis;
5. execute investigação;
6. selecione implementation owner;
7. execute reviewers;
8. execute tests/gates;
9. trate findings;
10. atualize backlog;
11. selecione próximo READY;
12. continue automaticamente.

Não criar arquitetura paralela.

Use a que já existe.

======================================================================
12. REGRA DE IMPLEMENTAÇÃO
======================================================================

Se existe `implementation-engineer` ou equivalente:

ele deve continuar sendo o único writer principal quando o protocolo assim determinar.

Reviewers read-only devem permanecer read-only quando hard enforcement existir.

Se a restrição for apenas textual:

registre:

`READ_ONLY_ENFORCEMENT = PARTIAL`

Não finja isolamento que o runtime não oferece.

======================================================================
13. AGENT ROUTING
======================================================================

Construa o routing map REAL:

TASK TYPE
→ PRIMARY AGENT
→ SUPPORTING AGENTS
→ SKILLS
→ REVIEWERS
→ GATES.

Baseado nos agents realmente encontrados.

Não use uma lista histórica de 16.

======================================================================
14. SKILL ROUTING
======================================================================

Construa:

AGENT
→ AVAILABLE SKILLS
→ WHEN USED.

Não invoque skill arbitrariamente apenas para aumentar contador.

Mas todas devem ser:

- descobertas;
- validadas;
- classificadas;
- disponíveis para roteamento.

======================================================================
15. STATUS DE CADA AGENT
======================================================================

Quero ao final desta fase:

AGENT
DISCOVERED
LOADABLE
INVOCATION MODE
VALIDATED
ROUTABLE
STATUS.

Status possíveis:

ACTIVE
AVAILABLE
NATIVE
EMULATED
PARTIAL
BROKEN
STALE
ORPHAN
NOT_SUPPORTED
BLOCKED.

======================================================================
16. STATUS DE CADA SKILL
======================================================================

SKILL
DISCOVERED
LOADABLE
VALIDATED
ROUTABLE
CONSUMERS
STATUS.

======================================================================
17. NÃO MODIFICAR DEFINITIONS SEM NECESSIDADE
======================================================================

O objetivo desta fase é:

ATIVAR E VALIDAR.

Não refatore agent prompts só porque encontrou melhoria possível.

Não recrie skills.

Não reorganize `.claude/`.

Se definição estiver realmente quebrada e impedir activation:

registre primeiro.

Corrija somente se dentro da maintenance já autorizada e necessária para funcionamento.

======================================================================
18. DESCOBRIR SE EXISTE COMANDO DE TERMINAL
======================================================================

RESPONDA COM EVIDÊNCIA:

Existe um comando canônico de terminal para ativar o sistema inteiro?

Use:

`CANONICAL_AUTONOMY_COMMAND = <command>`

OU:

`CANONICAL_AUTONOMY_COMMAND = NONE`

Se existir:

prove pelo source.

Se não existir:

não invente.

Nesse caso explique qual é o activation path real:

Claude Code session
→ runtime
→ launcher/workflow
→ agent invocation.

======================================================================
19. SE EXISTIR LAUNCHER AUTÔNOMO
======================================================================

Se encontrar um launcher oficial realmente destinado à execução autônoma:

valide primeiro em dry-run/read-only ou modo seguro, se existir.

Depois execute-o SOMENTE se:

- não for destructive;
- não alterar git;
- não aplicar migrations não autorizadas;
- não acessar produção;
- não criar paralelismo inseguro.

Se não houver safe mode:

não invente.

======================================================================
20. PROIBIÇÕES
======================================================================

Não:

- commit;
- push;
- merge;
- rebase;
- stash;
- reset;
- clean;
- checkout;
- switch;
- editar main;
- executar migration alheia;
- tocar staging/prod;
- disparar 31 agents simultaneamente.

Branch:

`dev`.

======================================================================
21. GATE DE ATIVAÇÃO
======================================================================

Esta fase somente termina quando você puder provar:

- N agents reais descobertos;
- M skills reais descobertas;
- launchers inventariados;
- workflows inventariados;
- agent loader/invocation path conhecido;
- skill loader/invocation path conhecido;
- pelo menos 2 agents executados sequencialmente com sucesso;
- pelo menos 1 skill executada com sucesso;
- journal/state íntegros após as execuções;
- routing map criado;
- autonomous orchestrator path identificado;
- todos os agents classificados como routable/broken/etc.;
- todas as skills classificadas.

======================================================================
22. STATUS OBRIGATÓRIO
======================================================================

Somente depois use:

`CLAUDE AGENT SYSTEM ACTIVATED`

seguido de:

`DISCOVERED AGENTS: N`
`ROUTABLE AGENTS: X/N`

`DISCOVERED SKILLS: M`
`ROUTABLE SKILLS: Y/M`

`CANONICAL AUTONOMY COMMAND: ...`

`CONTROL-PLANE MODE: SINGLE-WRITER`

Não use:

`ALL ACTIVE`

se algum estiver broken/not-supported.

======================================================================
23. DEPOIS DA ATIVAÇÃO — CONTINUE SOZINHO
======================================================================

Quando o sistema estiver ativado e validado:

# NÃO PARE.

Retome automaticamente o trabalho interrompido.

Ordem:

CONTROL-PLANE MAINTENANCE
→ `.claude/` 13/13 AUDIT
→ MUSICCHAT REMAINING FINDINGS
→ FORCE RLS / OWNERSHIP
→ FULL TESTS
→ PLAYWRIGHT
→ INTEGRATIONS
→ FORENSIC PROJECT AUDIT
→ BACKLOG
→ NEXT READY.

Use os agents e skills recém-validados para executar essas fases.

======================================================================
24. NÃO ME PERGUNTE O PRÓXIMO PASSO
======================================================================

Minha autorização é explícita:

# ATIVE A INFRAESTRUTURA AGORA.

Depois:

# USE-A PARA CONTINUAR AUTONOMAMENTE.

Não fique:

“aguardando”
“holding”
“qual prioridade?”

Continue enquanto houver READY autorizado.

======================================================================
25. EXECUTE AGORA
======================================================================

PRIMEIRO:

1. inventarie `.claude/agents`;
2. conte N;
3. inventarie `.claude/skills`;
4. conte M;
5. audite `.claude/launchers`;
6. audite `.claude/workflows`;
7. descubra activation path;
8. descubra se existe comando canônico de terminal;
9. execute UM agent;
10. verify journal/state;
11. execute SEGUNDO agent;
12. verify journal/state;
13. execute UMA skill;
14. verify journal/state;
15. monte routing map;
16. marque agents/skills routable;
17. ative/use o orchestrator;
18. CONTINUE O TRABALHO.

# NÃO FAÇA MAIS NENHUMA TAREFA DE PRODUTO ANTES DE TERMINAR ESTE GATE.

# A PARTIR DE AGORA, OS AGENTS E SKILLS DEVEM SER REALMENTE UTILIZADOS NA CONTINUAÇÃO AUTÔNOMA.