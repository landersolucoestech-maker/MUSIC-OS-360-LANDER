{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://engineering-os/contracts/tool-manifest.schema.json",
  "title": "ToolManifest",
  "description": "Declares one tool's side-effect classification per .claude/rules/side-effects-recovery.md. Consumer: .claude/policies/capabilities.json entries reference a tool by name here; policy-engine.mjs uses the classification to decide whether an agent may be granted it without an approval-request.",
  "type": "object",
  "required": ["name", "classification"],
  "properties": {
    "name": { "type": "string" },
    "classification": {
      "enum": ["READ_ONLY", "LOCAL_WRITE", "REPOSITORY_WRITE", "EXTERNAL_WRITE", "INFRASTRUCTURE_WRITE", "PRODUCTION_WRITE", "DESTRUCTIVE", "IRREVERSIBLE"]
    },
    "requiresApproval": { "type": "boolean" }
  }
}
