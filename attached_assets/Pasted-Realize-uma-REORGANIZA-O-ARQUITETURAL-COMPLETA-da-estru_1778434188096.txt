Realize uma REORGANIZAÇÃO ARQUITETURAL COMPLETA da estrutura do MUSIC OS 360 transformando o projeto em uma arquitetura enterprise modular definitiva, mantendo TOTAL compatibilidade com o frontend atual, UX atual, renderizações atuais, formulários atuais e fluxos operacionais existentes.

IMPORTANTE:
O frontend é atualmente a principal referência operacional do sistema.

PORTANTO:

* NÃO quebrar UX;
* NÃO quebrar renderização;
* NÃO quebrar formulários;
* NÃO quebrar fluxos;
* NÃO alterar comportamento operacional;
* NÃO alterar experiência visual consolidada;
* NÃO alterar lógica funcional do módulo releases;
* NÃO alterar comportamento operacional do módulo releases;
* NÃO alterar estrutura visual do módulo releases.

OBJETIVO:
Consolidar a arquitetura enterprise real do sistema.

IMPLEMENTAR REORGANIZAÇÃO COMPLETA DAS PASTAS.

━━━━━━━━━━━━━━━━━━
ROOT STRUCTURE
━━━━━━━━━━━━━━━━━━

A estrutura final obrigatória deve ser:

```txt
client/src/
├── app/
├── modules/
├── shared/
├── integrations/
├── infrastructure/
├── config/
├── styles/
├── types/
├── assets/
├── workers/
└── test/
```

━━━━━━━━━━━━━━━━━━
APP LAYER
━━━━━━━━━━━━━━━━━━

Reorganizar app/ para:

```txt
app/
├── providers/
├── router/
├── layouts/
├── guards/
├── boot/
├── config/
├── state/
└── initialization/
```

REGRAS:

* app/ NÃO pode conter lógica de domínio;
* app/ NÃO pode conter business rules;
* app/ NÃO pode conter lógica de IA;
* app/ NÃO pode conter lógica operacional de módulos.

━━━━━━━━━━━━━━━━━━
MODULES
━━━━━━━━━━━━━━━━━━

Padronizar TODOS os módulos para:

```txt
modules/[module]/
├── application/
├── domain/
├── infrastructure/
├── presentation/
├── components/
├── hooks/
├── services/
├── providers/
├── analytics/
├── automations/
├── workflows/
├── integrations/
├── contracts/
├── validators/
├── schemas/
├── mappers/
├── state/
├── types/
├── tests/
└── index.ts
```

━━━━━━━━━━━━━━━━━━
REGRAS OBRIGATÓRIAS DOS MÓDULOS
━━━━━━━━━━━━━━━━━━

IMPLEMENTAR:

* boundaries reais;
* isolamento de domínio;
* desacoplamento;
* separation of concerns;
* module contracts;
* module public APIs.

PROIBIDO:

* módulo importar módulo diretamente;
* shared importar módulos;
* lógica de domínio dentro de shared;
* providers específicos dentro de shared;
* business rules espalhadas.

Comunicação entre módulos SOMENTE via:

* contracts;
* domain events;
* orchestrators;
* APIs públicas;
* shared abstractions.

━━━━━━━━━━━━━━━━━━
SHARED LAYER
━━━━━━━━━━━━━━━━━━

Reorganizar shared/ para:

```txt
shared/
├── ui/
├── design-system/
├── hooks/
├── lib/
├── utils/
├── components/
├── providers/
├── infrastructure/
├── domain-events/
├── analytics/
├── observability/
├── auth/
├── permissions/
├── tenant/
├── feature-flags/
├── realtime/
├── storage/
├── schemas/
├── constants/
├── types/
├── governance/
└── testing/
```

━━━━━━━━━━━━━━━━━━
REGRAS DO SHARED
━━━━━━━━━━━━━━━━━━

Shared deve ser:

* agnóstico;
* desacoplado;
* reutilizável;
* sem conhecimento de domínio.

PROIBIDO:

* shared importar módulos;
* shared conhecer entidades específicas;
* shared conter regras de negócio;
* shared conter lógica operacional específica.

━━━━━━━━━━━━━━━━━━
DESIGN SYSTEM
━━━━━━━━━━━━━━━━━━

CRIAR:

```txt
shared/design-system/
├── tokens/
├── colors/
├── typography/
├── spacing/
├── shadows/
├── animations/
├── layouts/
├── forms/
├── tables/
├── modals/
├── date-pickers/
├── charts/
├── states/
├── feedback/
├── icons/
└── themes/
```

PADRONIZAR:

* spacing;
* semantic colors;
* typography;
* form patterns;
* modal patterns;
* table patterns;
* loading states;
* feedback states;
* date pickers;
* cards;
* dashboards.

━━━━━━━━━━━━━━━━━━
AI PLATFORM
━━━━━━━━━━━━━━━━━━

Consolidar:

```txt
modules/ai/
├── application/
├── domain/
├── infrastructure/
├── presentation/
├── providers/
├── orchestrators/
├── skills/
├── automations/
├── agents/
├── workflows/
├── queues/
├── workers/
├── execution/
├── analytics/
├── observability/
├── memory/
├── governance/
├── prompts/
├── parsers/
├── validators/
├── contracts/
├── state/
├── types/
└── tests/
```

━━━━━━━━━━━━━━━━━━
IA — IMPLEMENTAR CORRETAMENTE
━━━━━━━━━━━━━━━━━━

Separar corretamente:

* prompts;
* providers;
* orchestrators;
* execution;
* workers;
* analytics;
* workflows;
* agents.

PROIBIDO:

* IA executando diretamente no frontend;
* prompts hardcoded em componentes;
* providers diretamente em páginas;
* lógica de IA espalhada.

━━━━━━━━━━━━━━━━━━
WORKERS
━━━━━━━━━━━━━━━━━━

CRIAR:

```txt
workers/
├── ai/
├── analytics/
├── integrations/
├── automations/
├── webhooks/
└── processing/
```

IMPLEMENTAR PREPARAÇÃO PARA:

* BullMQ;
* Redis;
* retries;
* async processing;
* distributed execution.

━━━━━━━━━━━━━━━━━━
INFRASTRUCTURE
━━━━━━━━━━━━━━━━━━

CRIAR:

```txt
infrastructure/
├── api/
├── queue/
├── websocket/
├── workers/
├── redis/
├── cache/
├── monitoring/
├── logging/
├── telemetry/
├── storage/
├── auth/
├── database/
├── ai/
└── integrations/
```

━━━━━━━━━━━━━━━━━━
INTEGRATIONS
━━━━━━━━━━━━━━━━━━

Padronizar:

```txt
integrations/
├── clerk/
├── stripe/
├── resend/
├── autentique/
├── posthog/
├── sentry/
├── cloudflare-r2/
├── instagram/
├── tiktok/
├── spotify/
├── youtube/
├── deezer/
├── apple-music/
├── soundcloud/
├── google/
├── ecad/
├── ubc/
└── abramus/
```

REGRA:
Integrações NÃO podem conter:

* business rules;
* lógica operacional;
* lógica de domínio.

Somente:

* clients;
* SDKs;
* adapters;
* auth;
* requests;
* mappers.

━━━━━━━━━━━━━━━━━━
CORREÇÕES OBRIGATÓRIAS
━━━━━━━━━━━━━━━━━━

MOVER:

```txt
shared/pages/Dashboard
→ modules/dashboard/
```

MOVER:

```txt
shared/hooks/useMetrics
→ modules/dashboard/hooks/
```

MOVER:

```txt
shared/components/ContratoStatusBadge
→ modules/contracts/components/
```

FUNDIR:

```txt
modules/rights-monitoring/
```

em:

```txt
modules/monitoring/
```

DEFINIR boundaries corretos.

━━━━━━━━━━━━━━━━━━
BOUNDARIES
━━━━━━━━━━━━━━━━━━

IMPLEMENTAR:

* eslint boundaries;
* dependency rules;
* import restrictions;
* architecture enforcement.

PROIBIR:

* imports cruzados;
* shared importando módulos;
* módulos acessando internals de outros módulos.

━━━━━━━━━━━━━━━━━━
PRESERVAÇÃO OBRIGATÓRIA
━━━━━━━━━━━━━━━━━━

NÃO QUEBRAR:

* fluxos;
* UX;
* formulários;
* renderização;
* operações;
* dashboards;
* relacionamento entre módulos;
* navegação;
* estrutura visual;
* comportamento atual.

━━━━━━━━━━━━━━━━━━
MÓDULO RELEASES
━━━━━━━━━━━━━━━━━━

PROIBIDO:

* alterar lógica;
* alterar visual;
* alterar renderização;
* alterar UX;
* alterar formulários;
* alterar estrutura operacional.

Somente:

* organizar estrutura externa;
* preservar integralmente comportamento atual.

━━━━━━━━━━━━━━━━━━
VALIDAR APÓS REORGANIZAÇÃO
━━━━━━━━━━━━━━━━━━

Verificar:

* imports quebrados;
* circular dependencies;
* boundaries;
* renderizações;
* formulários;
* hooks;
* providers;
* roteamento;
* performance;
* build;
* tipagem;
* lazy loading;
* code splitting.

━━━━━━━━━━━━━━━━━━
RESULTADO FINAL OBRIGATÓRIO
━━━━━━━━━━━━━━━━━━

O sistema deve terminar:

* modular;
* desacoplado;
* enterprise-ready;
* escalável;
* preparado para IA massiva;
* preparado para automações;
* preparado para multi-tenant real;
* preparado para observabilidade;
* preparado para crescimento internacional;
* preparado para milhares de usuários simultâneos;
* preparado para monetização enterprise;
* preparado para backend enterprise real.

Gerar ao final:

* relatório completo da reorganização;
* o que foi movido;
* o que foi reorganizado;
* o que foi desacoplado;
* o que foi padronizado;
* o que ainda falta consolidar;
* riscos restantes;
* próximos passos arquiteturais.
