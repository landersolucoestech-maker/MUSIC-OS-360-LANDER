ANALISE E EXECUTE AS CORREÇÕES ABAIXO NO PROJETO SEM QUEBRAR A ESTRUTURA EXISTENTE. 
O OBJETIVO É DEIXAR O BACKEND TOTALMENTE FUNCIONAL, CONECTADO E PRONTO PARA PRODUÇÃO/TESTES REAIS.

==================================================
CONTEXTO ATUAL DO PROBLEMA
==================================================

O projeto possui frontend + backend, porém:

- O backend NestJS está crashando porque as variáveis de ambiente estão vazias/inválidas.
- O frontend funciona apenas porque está em MOCK_MODE.
- O banco ainda não foi provisionado.
- O Vite está configurado incorretamente usando aiApiPlugin() chamando OpenAI direto no frontend.
- O proxy da API está errado.

==================================================
OBJETIVO
==================================================

Deixar o sistema operacional com:

- Backend NestJS funcionando
- Banco Neon conectado
- Clerk funcionando
- Redis Upstash funcionando
- Drizzle sincronizado
- Frontend conectado corretamente no backend
- Proxy funcionando
- MOCK_MODE opcional
- Estrutura limpa e organizada

==================================================
TAREFA 1 — CONFIGURAR VARIÁVEIS DE AMBIENTE DO BACKEND
==================================================

Abrir:

apps/api/.env

Substituir os placeholders atuais por variáveis reais.

Atualmente está assim:

NEON_DATABASE_URL=
CLERK_SECRET_KEY=sk_test_placeholder
REDIS_QUEUE_URL=redis://localhost:6379

Problemas:
- Banco inexistente
- Clerk inválido
- Redis local não existe no Replit

==================================================
AÇÃO NECESSÁRIA
==================================================

1. Criar e configurar:
   - Neon PostgreSQL
   - Clerk
   - Upstash Redis

2. Preencher corretamente:

NEON_DATABASE_URL=
CLERK_SECRET_KEY=
REDIS_QUEUE_URL=

3. Validar:
   - conexão PostgreSQL
   - conexão Redis
   - autenticação Clerk

4. Garantir que o NestJS suba sem crash.

==================================================
TAREFA 2 — SINCRONIZAR BANCO COM DRIZZLE
==================================================

Executar:

npm run db:push

Objetivo:
- criar todas as tabelas automaticamente no Neon.

IMPORTANTE:
- O schema Drizzle já existe.
- NÃO alterar schema sem necessidade.
- NÃO apagar migrations existentes.
- Apenas sincronizar corretamente.

Validar:
- tabelas criadas
- sem erro de conexão
- sem conflito de migration

==================================================
TAREFA 3 — CONFIGURAR .ENV DO FRONTEND
==================================================

Criar arquivo:

.env

na raiz do projeto.

Adicionar:

VITE_CLERK_PUBLISHABLE_KEY=
VITE_USE_MOCK=true

Regras:
- usar a publishable key correta do Clerk
- manter MOCK_MODE habilitado inicialmente
- garantir compatibilidade futura para desligar mocks

Validar:
- frontend inicia
- Clerk inicializa corretamente
- sem erro de auth no navegador

==================================================
TAREFA 4 — CORRIGIR vite.config.ts
==================================================

Abrir:

vite.config.ts

==================================================
PROBLEMA 1
==================================================

Existe:

aiApiPlugin()

Isso está errado porque:
- frontend chama OpenAI diretamente
- bypassa o backend
- expõe arquitetura insegura
- quebra separação backend/frontend

==================================================
AÇÃO
==================================================

REMOVER COMPLETAMENTE:

aiApiPlugin()

e qualquer import relacionado.

Garantir:
- nenhum código restante dependa dele
- build continue funcionando

==================================================
PROBLEMA 2
==================================================

O proxy do Vite está incorreto.

==================================================
AÇÃO
==================================================

Corrigir proxy para apontar corretamente para:

/api/v1

Exemplo esperado:

server: {
  proxy: {
    '/api': {
      target: 'http://localhost:3000',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, '/api/v1'),
    },
  },
}

Validar:
- requests frontend chegam no NestJS
- rotas respondem corretamente
- sem CORS manual desnecessário

==================================================
VALIDAÇÕES FINAIS OBRIGATÓRIAS
==================================================

Após todas as correções:

1. Executar backend
2. Executar frontend
3. Validar:
   - conexão banco
   - auth Clerk
   - Redis
   - proxy Vite
   - chamadas API
   - Drizzle
   - sem crashes

==================================================
RESTRIÇÕES IMPORTANTES
==================================================

- NÃO remover arquitetura existente.
- NÃO reescrever o sistema.
- NÃO alterar regras de negócio.
- NÃO criar soluções mockadas adicionais.
- NÃO usar gambiarra.
- NÃO expor secrets no frontend.
- NÃO usar localhost Redis.
- NÃO usar SQLite.

==================================================
RESULTADO ESPERADO
==================================================

Entregar:

- backend funcional
- frontend funcional
- banco conectado
- auth operacional
- Redis operacional
- Drizzle sincronizado
- proxy correto
- sem crashes
- sem placeholders inválidos
- sem dependências quebradas

Ao finalizar:
- listar tudo que foi alterado
- listar arquivos modificados
- listar variáveis necessárias
- informar qualquer pendência restante
- informar comandos usados