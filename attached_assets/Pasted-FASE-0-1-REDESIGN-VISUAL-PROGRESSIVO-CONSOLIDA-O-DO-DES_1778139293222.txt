FASE 0.1 — REDESIGN VISUAL PROGRESSIVO + CONSOLIDAÇÃO DO DESIGN SYSTEM
CONTEXTO

A fundação arquitetural da FASE 0 foi executada corretamente:

TenantContext criado;
RBAC estruturado;
feature flags implementadas;
arquitetura multi-tenant preparada;
integrações desacopladas;
storage preparado;
design system iniciado.

Agora o objetivo NÃO é criar novas funcionalidades.

O objetivo desta fase é:

CONSOLIDAR VISUALMENTE O SISTEMA INTEIRO

Aplicando o novo Design System enterprise em TODAS as páginas, componentes e fluxos existentes.

OBJETIVO PRINCIPAL

Transformar visualmente o sistema atual em um SaaS enterprise premium com padrão:

Linear;
Stripe;
Vercel;
Notion;
Arc;
Attio.

Eliminando completamente:

aparência de template;
aparência genérica;
inconsistências visuais;
componentes improvisados;
layouts antigos;
páginas visualmente amadoras.
DIRETRIZ CRÍTICA

NÃO criar novas páginas do zero sem necessidade.

NÃO reescrever módulos inteiros.

O trabalho deve ser:

progressivo;
incremental;
consistente;
reutilizando a estrutura existente.

Tudo deve ser refatorado visualmente utilizando:

os patterns do Design System;
os novos tokens;
os novos componentes;
a nova hierarquia visual.
OBJETIVO TÉCNICO

Aplicar corretamente os patterns já criados:

CARD
TABLE
PAGE
STAT
EMPTY STATE
SECTION
FORM
MODAL
DRAWER
BADGE
STATUS
TOOLBAR
HEADER
FILTER BAR
PRIORIDADE DE EXECUÇÃO

A execução deve seguir exatamente esta ordem:

1. Dashboard
2. Artistas
3. Catálogo
4. Contratos
5. CRM
6. Marketing
7. Analytics
8. Onboarding
9. Financeiro
10. Configurações

NÃO avançar para o próximo módulo sem consolidar visualmente o anterior.

1. DASHBOARD — REDESIGN ENTERPRISE
OBJETIVO

Transformar o dashboard em um painel enterprise premium.

IMPLEMENTAR
Cards estatísticos

Refatorar:

espaçamento;
tipografia;
ícones;
alinhamento;
hover;
profundidade visual;
hierarchy.

Os STAT cards devem parecer:

Stripe;
Linear;
Vercel.
Gráficos

Padronizar:

grid;
legendas;
tipografia;
containers;
estados;
spacing.

Evitar:

gráficos espremidos;
excesso de cor;
aparência bootstrap/admin template.
Containers

Padronizar:

alturas;
padding;
radius;
bordas;
sombras;
background layers.
Header do dashboard

Refatorar:

título;
subtítulo;
ações rápidas;
filtros;
período;
contextualização do tenant.
2. PÁGINAS LISTAGEM

Aplicar novo padrão em:

artistas;
contratos;
CRM;
onboarding;
financeiro;
catálogo.
TABLE PATTERN

Todas as tabelas devem seguir padrão enterprise:

cabeçalho minimalista;
linhas compactas;
hover states suaves;
badges elegantes;
ações contextuais;
spacing consistente.

Evitar:

tabelas pesadas;
excesso de borda;
aparência bootstrap.
FILTER BAR

Padronizar:

busca;
filtros;
selects;
ações rápidas;
ordenação.
EMPTY STATES

Criar empty states premium:

ilustrados;
contextuais;
úteis;
sofisticados.
3. FORMULÁRIOS

Todos os formulários devem ser refatorados.

INPUTS

Padronizar:

altura;
bordas;
estados;
labels;
helper text;
validação visual.
SECTIONS

Organizar formulários por:

blocos;
contexto;
agrupamento lógico.

Evitar:

formulários gigantes;
alinhamento ruim;
excesso de campos sem hierarquia.
DRAWERS & MODALS

Padronizar:

largura;
padding;
hierarquia;
ações;
footer fixo;
animações suaves.
4. NAVEGAÇÃO GLOBAL

Consolidar:

sidebar;
submenus;
breadcrumbs;
topbar;
ações contextuais.
SIDEBAR

A sidebar já foi iniciada corretamente.

Agora:

padronizar espaçamentos;
animações;
estados ativos;
estados hover;
collapses;
comportamento responsivo.
SUBMENUS

Submenus devem seguir:

estilo Linear;
borda lateral;
compactação premium;
animações suaves.
5. DARK MODE PREMIUM

Refinar completamente o dark mode.

OBJETIVO

Evitar:

preto absoluto;
contraste exagerado;
aparência neon;
excesso de brilho.

Criar:

profundidade sofisticada;
camadas sutis;
sombras suaves;
contraste premium.
6. RESPONSIVIDADE REAL

O sistema NÃO pode parecer apenas “adaptado”.

Deve existir:

experiência mobile real;
experiência tablet;
adaptação contextual;
collapse inteligente;
reorganização de grids.
7. CONSOLIDAÇÃO DO DESIGN SYSTEM

Todos os módulos devem consumir:

os mesmos tokens;
os mesmos patterns;
os mesmos componentes;
os mesmos espaçamentos;
as mesmas regras visuais.
PROIBIÇÕES

NÃO:

criar estilos inline aleatórios;
criar componentes fora do design system;
duplicar componentes;
criar telas visualmente diferentes do padrão;
misturar padrões antigos e novos;
usar cores aleatórias;
usar spacing inconsistente.
RESULTADO ESPERADO

Ao finalizar esta fase:

TODAS as páginas devem parecer parte do mesmo produto;
o sistema deve parecer um SaaS enterprise premium real;
a UX deve transmitir software corporativo profissional;
a aparência de template/admin panel deve desaparecer completamente;
o sistema deve parecer pronto para comercialização.