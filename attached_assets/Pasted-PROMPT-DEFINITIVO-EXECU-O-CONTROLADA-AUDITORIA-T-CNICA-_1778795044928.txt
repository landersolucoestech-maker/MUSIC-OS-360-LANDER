PROMPT DEFINITIVO — EXECUÇÃO CONTROLADA, AUDITORIA TÉCNICA E ESTABILIZAÇÃO DO MUSIC OS 360
LEIA ISTO ANTES DE EXECUTAR QUALQUER AÇÃO

Você NÃO possui autonomia arquitetural.

Você NÃO deve:

reinterpretar o produto;
reinventar a arquitetura;
reorganizar o sistema arbitrariamente;
aplicar “boas práticas genéricas” sem necessidade real;
criar abstrações artificiais;
modernizar o sistema por opinião;
alterar UX consolidada;
alterar fluxos existentes;
alterar comportamento visual;
alterar estrutura sem necessidade crítica comprovada.

Seu papel é:

estabilizar;
corrigir;
validar;
proteger;
otimizar incrementalmente;
preservar integralmente o funcionamento atual.
FONTES OFICIAIS DE VERDADE

As seguintes documentações são normativas e obrigatórias:

GOVERNANCE.md
MAPEAMENTO_ESTRUTURAL.md
.env.example
EXECUTION.md

Você deve seguir rigorosamente:

stack definido;
arquitetura definida;
módulos definidos;
naming definido;
rotas definidas;
providers definidos;
estrutura definida.
REGRA ABSOLUTA PRINCIPAL

O frontend atual é a referência operacional oficial do sistema.

PRESERVAR INTEGRALMENTE:

UX;
renderização;
formulários;
fluxos;
comportamento visual;
responsividade;
autocomplete;
preenchimento automático;
lógica operacional;
experiência desktop;
experiência mobile;
experiência dos módulos.
PROIBIÇÕES ABSOLUTAS
PROIBIDO
mover arquivos entre módulos sem necessidade crítica;
reorganizar arquitetura arbitrariamente;
alterar rotas;
alterar slugs;
alterar contratos de componentes;
alterar UX consolidada;
alterar comportamento visual;
trocar bibliotecas;
instalar frameworks alternativos;
alterar schemas sem migration explícita;
recriar componentes existentes;
recriar hooks existentes;
recriar providers existentes;
criar wrappers desnecessários;
criar abstrações genéricas artificiais;
criar microserviços;
criar arquitetura hexagonal;
criar event sourcing;
criar CQRS complexo;
criar repository pattern sem necessidade real;
criar engines genéricas;
criar “enterprise boilerplate”;
remover MOCK_MODE;
quebrar frontend standalone;
alterar fluxo do módulo de lançamentos.
STACK OFICIAL — NÃO QUESTIONAR
FRONTEND
React 18
TypeScript strict
Vite + SWC
TanStack Query v5
React Router v6
shadcn/ui
Radix UI
Tailwind CSS
Zod
React Hook Form
Recharts
Sonner
BACKEND
NestJS
Drizzle ORM
Neon PostgreSQL
BullMQ
Railway Redis
Upstash Redis
Clerk
Stripe
Cloudflare R2
OpenAI
Claude
Gemini
Resend
PostHog
Sentry
TECNOLOGIAS ABSOLUTAMENTE PROIBIDAS
NUNCA USAR
Supabase
Docker
Docker Compose
Firebase
Prisma
MongoDB
Next.js
Zustand
Redux
Sequelize
MikroORM
OBJETIVO DA EXECUÇÃO

Executar auditoria técnica completa e estabilização incremental do SaaS MUSIC OS 360 preservando integralmente:

arquitetura;
UX;
estrutura;
funcionamento;
comportamento operacional;
módulos existentes.
PRIORIDADE OFICIAL DE EXECUÇÃO
PRIORIDADE 1 — CRÍTICO

Corrigir primeiro:

segurança;
multi-tenant isolation;
tenant guards;
auth;
autorização;
vazamento de dados;
queries inseguras;
webhooks;
uploads;
secrets expostos;
falhas críticas;
corrupção de dados.
PRIORIDADE 2 — ESTABILIDADE

Corrigir:

crashes;
loops;
race conditions;
memory leaks;
falhas de filas;
falhas websocket;
falhas de cache;
retries;
sincronização.
PRIORIDADE 3 — PERFORMANCE

Corrigir:

lazy loading;
Suspense;
chunk splitting;
N+1;
re-renderizações;
queries pesadas;
payloads excessivos;
gargalos reais.
PRIORIDADE 4 — PADRONIZAÇÃO

Corrigir:

naming;
imports;
barrel exports;
tipagem;
inconsistências leves;
duplicações reais.
PRIORIDADE 5 — UX

Corrigir APENAS:

inconsistências reais;
loading states;
empty states;
acessibilidade;
responsividade;
feedback visual ausente.

SEM alterar UX consolidada.

EXECUÇÃO OBRIGATÓRIA — PASSO A PASSO
PASSO 1 — LIMPEZA CRÍTICA

EXECUTAR IMEDIATAMENTE:

DELETAR
infra/docker-compose.yml
qualquer Dockerfile
qualquer .dockerignore
REMOVER
qualquer referência a Supabase;
qualquer SUPABASE_URL;
qualquer SUPABASE_SERVICE_ROLE_KEY;
qualquer key lander_*;
qualquer cookie lander360_rt.
VALIDAR
cookie oficial = musicos360_rt
localStorage oficial = musicos360_mock_data
CustomEvents = musicos360:*
PASSO 2 — AUDITORIA FRONTEND

Analisar:

client/src/
módulos;
hooks;
pages;
providers;
shared;
routes;
forms;
mappers;
schemas;
modais;
tabelas;
lazy loading;
renderização;
Suspense;
DataTable;
PageHeader;
EmptyState;
Skeletons;
RHF;
Zod;
TanStack Query.
VALIDAR OBRIGATORIAMENTE
TODOS OS FORMULÁRIOS

Devem usar:

react-hook-form
zodResolver

PROIBIDO:

formulários controlados manuais;
validação inline.
TODOS OS MAPPERS

Devem ser fonte única de verdade:

entidade → form
form → payload
TODOS OS HOOKS

Devem usar:

useDataQuery.ts

como camada central.

TODAS AS ROTAS

Devem usar:

React.lazy
<Suspense fallback={<PageSkeleton />}>
PASSO 3 — AUDITORIA BACKEND

Analisar:

guards;
interceptors;
services;
controllers;
DTOs;
queues;
cache;
auth;
RBAC;
tenant isolation;
uploads;
webhooks;
observabilidade.
VALIDAR OBRIGATORIAMENTE
TODOS OS ENDPOINTS

Devem possuir:

ClerkAuthGuard
TenantGuard
RolesGuard

quando aplicável.

TODOS OS SERVICES

Devem possuir:

WHERE tenant_id = :tenantId

em TODAS as queries.

CONTROLLERS

PROIBIDO:

lógica de negócio;
regras de domínio;
processamento pesado.

Controllers devem apenas:

receber request;
validar;
delegar;
responder.
PASSO 4 — AUDITORIA MULTI-TENANT

VALIDAR:

isolamento completo;
RBAC;
feature flags;
cache isolation;
Redis prefix;
tenant_id;
auth claims;
org_id;
queries;
uploads;
websocket isolation.
PASSO 5 — AUDITORIA BANCO

Analisar:

schema.ts
migrations
índices
relacionamentos
constraints
soft delete
foreign keys
tenant_id
encryption fields.
VALIDAR OBRIGATORIAMENTE
TODAS AS TABELAS

Devem possuir:

tenant_id
created_at
updated_at
deleted_at
TODAS AS TABELAS DE DOMÍNIO

Devem possuir índice:

tenant_id
PASSO 6 — AUDITORIA SEGURANÇA

VALIDAR:

AES-256;
EncryptionService;
ValidationPipe;
whitelist;
forbidNonWhitelisted;
helmet;
CSP;
rate limiting;
webhook validation;
Stripe validation;
Clerk webhook validation;
upload MIME validation;
upload size validation;
secret leakage;
logs sensíveis.
PASSO 7 — AUDITORIA PERFORMANCE

VALIDAR:

lazy loading;
manualChunks;
query optimization;
pagination;
re-renderizações;
websocket efficiency;
cache strategy;
TanStack Query config;
memoization necessária;
Suspense;
chunk splitting.
PASSO 8 — AUDITORIA UX/UI

VALIDAR:

PageHeader;
DataTable;
StatusBadge;
ContratoStatusBadge;
modais;
responsividade;
loading states;
empty states;
toasts;
mobile;
desktop;
grids;
spacing;
typography;
hierarchy;
consistency.
MÓDULO CRÍTICO — LANÇAMENTOS

NÃO ALTERAR:

fluxo;
UX;
validações;
autocomplete;
preenchimento;
relacionamento;
renderização.

Fluxo obrigatório:

Album Info
Track Upload
Album Art
Distribution Preferences
Preview
MOCK_MODE — REGRA ABSOLUTA

MOCK_MODE deve continuar funcionando integralmente.

NÃO QUEBRAR:

VITE_USE_MOCK=true

Frontend deve continuar:

standalone;
funcional;
operacional;
sem backend.
POLÍTICA DE ALTERAÇÃO

Toda alteração deve ser:

mínima;
incremental;
reversível;
localizada;
compatível;
segura;
rastreável.
REGRA ANTI-OVERENGINEERING

PROIBIDO criar:

abstrações artificiais;
wrappers desnecessários;
service factories;
repository pattern inútil;
generic CRUD engines;
universal forms;
arquitetura enterprise fake;
boilerplate excessivo;
sistemas genéricos sem uso real.
REGRA DE REUTILIZAÇÃO

Antes de criar qualquer:

componente;
hook;
provider;
utilitário;
helper;
modal;
tabela;
mapper;
schema;

VERIFICAR se já existe solução reutilizável.

SAÍDA FINAL OBRIGATÓRIA

Gerar:

1. Diagnóstico Geral

Estado real do sistema.

2. Problemas Críticos

Classificar:

crítico;
alto;
médio;
baixo.

Para cada item:

arquivo;
linha;
impacto;
risco;
solução aplicada.
3. Gargalos de Escalabilidade

Listar:

limitações;
impacto;
risco;
solução.
4. Problemas UX/UI

Listar:

inconsistências;
fricções;
problemas operacionais;
problemas premium.
5. Débito Técnico

Listar:

duplicações;
acoplamentos;
abstrações ruins;
complexidade desnecessária.
6. Alterações Executadas

Listar EXATAMENTE:

arquivos criados;
arquivos alterados;
arquivos deletados;
configurações alteradas.
7. Roadmap Estratégico

Separar:

curto prazo;
médio prazo;
longo prazo.
8. Checklist Final

Confirmar:

MOCK_MODE funcionando;
multi-tenant isolado;
zero Supabase;
zero Docker;
zero lander_*;
lançamentos preservado;
UX preservada;
rotas preservadas;
stack preservado;
MUSIC OS 360 preservado como única marca.