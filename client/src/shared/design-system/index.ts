/**
 * shared/design-system/index.ts
 *
 * Barrel raiz do design system.
 *
 * Contém:
 *  tokens/      — spacing, radius, z-index scales
 *  colors/      — semantic color system
 *  typography/  — Plus Jakarta Sans + IBM Plex Mono
 *  spacing/     — 4px grid scale
 *  shadows/     — elevation system
 *  animations/  — durations, easings, keyframes
 *  layouts/     — grid, flex, container patterns
 *  forms/       — field layout, validation states
 *  tables/      — DataTable patterns
 *  modals/      — Dialog, Sheet patterns
 *  date-pickers/ — Calendar, DatePickerField
 *  charts/      — FinanceChart patterns
 *  states/      — loading, error, empty, skeleton
 *  feedback/    — Toast, Alert, Banner
 */
export {};
