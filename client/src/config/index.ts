/**
 * config/index.ts
 *
 * Configurações globais da aplicação.
 *
 * Contém:
 *  app.config.ts    — nome, versão, ambiente
 *  routes.config.ts — prefixos de rotas globais
 *  api.config.ts    — base URLs, timeouts
 *  ai.config.ts     — modelos, providers, limites
 *  feature-flags.ts — flags de funcionalidades
 */
export {};
