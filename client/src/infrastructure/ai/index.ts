/**
 * infrastructure/ai/index.ts
 *
 * AI infrastructure — proxy para /api/ai/generate, rate limiting, cost tracking.
 * MOCK_MODE: providers simulam respostas (modules/ai/providers/).
 */
export {};
